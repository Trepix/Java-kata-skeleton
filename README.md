# Java-kata-skeleton

It includes major testing dependencies and lombok.

## Set up
Inside the root folder where your code will be placed, run:

```bash
sh -c "$(wget --quiet https://raw.githubusercontent.com/Trepix/Java-kata-skeleton/main/install.sh -O -)"
```